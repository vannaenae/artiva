{
  "name": "artiva",
  "version": "0.1.0",
  "description": "CLI for connecting to and calling tools on the Stitch MCP server (https://stitch.googleapis.com/mcp)",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "artiva": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "dev": "tsx src/cli.ts"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "commander": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
